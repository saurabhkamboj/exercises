let userId = 3;

// TypeScript infers the number type for `userId`,
// therefore if a 'string' is assigned to it, a type error is shown, 
// given that 'string' cannot be assigned to a type 'number'.
